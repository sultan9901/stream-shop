# ==========================================================================
# STREAM CORPORATION — Alembic configuration.
#
# The database URL is NOT stored here. ``migrations/env.py`` reads it from
# ``app.config.settings``, which reads it from the environment / .env, so a
# production DSN with a password never lands in a tracked file.
# ==========================================================================
[alembic]
script_location = migrations
prepend_sys_path = backend
path_separator = os
version_path_separator = os
# timestamped, sortable revision filenames
file_template = %%(year)d%%(month).2d%%(day).2d_%%(hour).2d%%(minute).2d_%%(rev)s_%%(slug)s

[post_write_hooks]
# none — revisions are reviewed by hand

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
